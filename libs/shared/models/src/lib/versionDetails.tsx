export interface VersionDetails {
    BACKEND?: string,
    MQTT_DATALOGGER?: string,
    MQTT_SPS_ADAPTER?: string,
    FTP_WATCHDOG?: string,
    FRONTEND?: string,
    C8Y_AGENT?: string,
    DPM_VERSION?: string,
}